- defaults:
    link:
      relink: true

- shell:
    - [echo "Executing osx.yaml"]

- link:
    ~/bin:
        path: bin-home
        force: true
    ~/.iterm2:
        clean: true
    ~/.iterm2_shell_integration.zsh:

# Run brew (slow)
# - brewfile:
#     file: Brewfile
#     no-upgrade: true
