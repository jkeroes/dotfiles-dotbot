- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles-dotbot: ''
    ~/.alias:
    ~/.zlogin:
    ~/.zprofile:
    ~/.zsh_favlist:
    ~/.zshenv:
    ~/.zshfunc:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
