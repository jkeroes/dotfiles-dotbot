- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles-dotbot: ''
    ~/.config:
        create: true
    ~/.alias:
    ~/.gitconfig:
    ~/.oh-my-zsh:
    ~/.oh-my-zsh/custom/themes/spaceship.zsh-theme:
        path: config/spaceship-prompt/spaceship.zsh-theme
    ~/.zlogin:
    ~/.zprofile:
    ~/.zsh_favlist:
    ~/.zshenv:
    ~/.zshfunc:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
